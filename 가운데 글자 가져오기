class Solution {
    public String solution(String s) {
        String answer = "";
        int length=s.length()/2;
        answer = (s.length() % 2==1) ? s.substring(length,length+1) : s.substring(length-1,length+1); 
        
        return answer;
    }
}
******************************************
글자 수에 따라 자르는 위치가 다르다
